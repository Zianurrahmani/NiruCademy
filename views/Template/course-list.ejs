<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List Report</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
</head>
<body>
  
  <%- include('partials/_navbar') %>  

  <h1 style="text-align: center;">Course List</h1>

  <div style="text-align: center;">
    <form action="/reports" method="get">
      <input type="text" name="event" placeholder="Search by event.." value="<%= event %>">
      <input type="number" name="age" placeholder="by age" value="<%= age %>">
      <input type="submit" value="Search"></input>
    </form>
  </div>
  
  <br>

  <div style="text-align: center; background-color: rgb(255, 249, 162);">
    <p >The oldest person is <%= oldest %> years old, the youngest is <%= youngest %> years old, and average all age is <%= Math.round(averageAge) %> years old</p>
  </div>
  <br>

  <table class="table table-bordered table-stripped">
    <thead>
    <tr>
      <th>No</th>
      <th>Course Name</th>
      <th>Category</th>
      <th>Price</th>
      <th>Actions</th>
    </tr>
    </thead>

    <tbody>
    
      <% courses.forEach(el => { %>
        <tr>
          <td><%= el.id %></td>
          <td><%= el.name %></td>
          <td><%= el.Category.name %></td>
          <td><%= el.price %></td>
          <td>
            <a href="/reports/<%= el.id %>">See Detail</a> | 
            <a href="/reports/<%= el.id %>/delete">Buy</a>
          </td>
        </tr>
      <% }) %>
        
    </tbody>
  
  </table>




</body>
</html>